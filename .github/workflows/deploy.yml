# name: "Deploy Spectron"
# run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
# on: [push]
# jobs:
#   PrintDeploymentStatus:
#     runs-on:  ubuntu-latest
#     steps:
#       - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
#   DeployToRemote:
#     runs-on:  ubuntu-latest
#     steps:
#       - run: echo "😂 Hassan sucks ${{ secrets.SSH_USERNAME }} ${{ secrets.SSH_HOST }}"
#       - run: ssh -i ${{ secrets.SSH_PUBLIC_KEY }} ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }}

name: "Deploy Spectron"
on:
  push:
    branches:
      - main

jobs:
  PrintDeploymentStatus:
    runs-on: ubuntu-latest
    steps:
      - name: Display Event Trigger
        run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."

  DeployToRemote:
    runs-on: ubuntu-latest
    needs: [PrintDeploymentStatus]
    steps:
      - name: SSH into the Remote Server and Update Repo
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd cd public_html/
            git pull origin main 
