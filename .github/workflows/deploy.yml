name: "Deploy Spectron"
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: multiple command
        uses: JimCronqvist/action-ssh@master
        with:
          hosts: 'spectrondigital.xyz@ssh.gb.stackcp.com'
          privateKey: ${{ secrets.SSH_PRIVATE_KEY }}
          command: |
            ls -lah
            cd public_html
            git pull origin main
          debug: true
