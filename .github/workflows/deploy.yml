name: "Deploy Spectron"
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  PrintDeploymentStatus:
    runs-on:  ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
  # DeployToRemote:
  #   runs-on:  ubuntu-latest
  #   steps:
  #     - run: echo "😂 Hassan sucks ${{ secrets.SSH_USERNAME }} ${{ secrets.SSH_HOST }}"
  #     - run: ssh -i ${{ secrets.SSH_PUBLIC_KEY }} ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }}